#include <iostream>
#include <string>
// #include "ScavTrap.hpp"
#include "FragTrap.hpp"

int main()
{
    // ClapTrap trap("ayoub");
    // trap.attack("Dummy");
    // trap.beRepaired(5);

    // trap.takeDamage(4);
    // trap.takeDamage(10);
    // trap.attack("Dummy");
    // trap.beRepaired(10);
    // for (int i = 0; i < 3; i++) {
    //     trap.attack("Enemy");
    //     trap.beRepaired(2);
    // }

    FragTrap frag("frag");
    // FragTrap f("f");
    // frag.takeDamage(10);
    // frag.beRepaired(1);
    // frag = f;
    // frag.beRepaired(1);
    // frag.beRepaired(1);

    

    
}